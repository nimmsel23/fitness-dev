#!/usr/bin/env python3
"""fitness-tui — Entry-Point für fitness_cli.commands.tui."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from fitness_cli.commands.tui import main

if __name__ == "__main__":
    main()
