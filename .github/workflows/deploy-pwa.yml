name: Deploy Fitness PWA

on:
  push:
    branches: [master]
    paths: [pwa/**]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: pwa/package-lock.json

      - name: Firebase config schreiben
        run: echo "$FIREBASE_CONFIG" > pwa/src/firebase.config.js
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}

      - name: Install
        run: npm ci
        working-directory: pwa

      - name: Build
        run: npm run build
        working-directory: pwa

      - name: Deploy to Firebase Hosting
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > /tmp/sa.json
          GOOGLE_APPLICATION_CREDENTIALS=/tmp/sa.json \
            npx firebase-tools@latest deploy --only hosting,firestore:rules,firestore:indexes \
            --project fitness-aos --non-interactive
        working-directory: pwa
