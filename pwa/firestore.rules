rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-user, kein Auth — für Prod absichern
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
