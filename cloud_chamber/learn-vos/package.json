{
  "name": "learn-dev",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node ../../server.mjs & npx vite --config vite.config.js",
    "build": "npx vite build --config vite.config.js",
    "build:firebase": "npx vite build --mode firebase --config vite.config.js && cp dist-firebase/cloud_chamber/learn-dev/index.html dist-firebase/index.html",
    "deploy": "npm run build:firebase && firebase deploy --only hosting",
    "preview": "npx vite preview --config vite.config.js"
  }
}
